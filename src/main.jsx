import React from 'react'
import ReactDOM from 'react-dom/client'
import { GifVisualizer } from './Components/GifVisualizer'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <GifVisualizer />
  </React.StrictMode>,
)
